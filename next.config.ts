import type {NextConfig} from "next";

/**
 * Next.js設定オブジェクト
 * Next.jsアプリケーションの動作をカスタマイズするための設定
 *
 * @remarks
 * 現在はデフォルト設定を使用している
 * 必要に応じてここにビルドオプションやその他の設定を追加できる
 */
const nextConfig: NextConfig = {
    /* config options here */
};

export default nextConfig;
